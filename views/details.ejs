<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TeamCraft - Profil Detayı</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1><%= developer.name %></h1>
        <div class="profile-details">
            <p><strong>Yetenekler:</strong> <%= developer.skills %></p>
            <p><strong>LinkedIn:</strong> <a href="<%= developer.linkedin %>" target="_blank"><%= developer.linkedin %></a></p>
            <p><strong>Profil Oluşturulma Tarihi:</strong> <%= developer.createdAt.toLocaleDateString('tr-TR') %></p>
        </div>

        <!-- Silme Butonu bir form içinde olmalı -->
        <form action="/developers/delete/<%= developer._id %>" method="POST">
            <button class="delete-button">Profili Sil</button>
        </form>
        <br>
        <a href="/developers">Tüm Listeye Geri Dön</a>
    </div>
</body>
</html>